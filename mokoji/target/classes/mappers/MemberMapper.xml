<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MemberDAO">

	<select id="checkMember" parameterType="member" resultType="member">
		SELECT MEM_ID, MEM_PW ,MEM_NAME FROM MEMBER WHERE MEM_ID = #{MEM_ID}
		<if test="MEM_PW != null">
			AND MEM_PW = #{MEM_PW}
		</if>
	</select>
	
	<insert id="insertMember" parameterType="member">
		INSERT INTO MEMBER
		(MEM_CODE, MEM_ID, MEM_PW, MEM_NAME, MEM_JUMIN, MEM_TEL, MEM_FAVORITE, MEM_POINT, MEM_LOC, MEM_PIC, MEM_ADDR)
		VALUES (seq_member.nextval(), #{MEM_ID}, #{MEM_PW}, #{MEM_NAME}, #{MEM_JUMIN}, #{MEM_TEL}, #{MEM_FAVORITE}, 0, #{MEM_LOC}, #{MEM_PIC}, #{MEM_ADDR})
	</insert>

</mapper>